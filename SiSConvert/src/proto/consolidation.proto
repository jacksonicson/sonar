package de.tum.in.dss.dbound;

message Consolidation {
	required string name = 1;
	
	required uint64 numServers = 2; 
	required uint64 numServices = 3;

	message Assignment {
		required uint64 server = 1; 
		required uint64 service = 2; 
	}	 
	
	repeated Assignment assignments = 100; 
}